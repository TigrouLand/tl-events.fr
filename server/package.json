{
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "exec": "node dist",
    "run": "yarn run clean && yarn run build && yarn run exec"
  },
  "dependencies": {
    "fastify": "^3.15.1",
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@types/node": "^15.3.0"
  }
}
